void preto_preto(){
			OnFwd(MOTOR_DIR_ESQ,30);
			/*OnFwd(MOTOR_DIR_ESQ,0);
			Wait (800);
			OnFwd(MOTOR_DIR_ESQ,45);
			Wait (300);
			
			if (brancoDir() && brancoEsq()){
			OnFwd(MOTOR_DIR_ESQ,-45);
			Wait (300);
			/*OnFwd(MOTOR_DIR,45);
			OnFwd(MOTOR_ESQ,0);
			Wait (1000);
			OnFwd(MOTOR_DIR_ESQ,0);
			Wait (800);
			if (brancoDir() && brancoEsq()){
			OnFwd(MOTOR_DIR,-45);
			OnFwd(MOTOR_ESQ,0);
			Wait (1050);
			OnFwd(MOTOR_DIR_ESQ,0);
			Wait (800);			
			}
			}
			*/
}

